# Migration `20200410085121-h`

This migration has been generated by Daniel Wallace at 4/10/2020, 8:51:21 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200410084607-5..20200410085121-h
--- datamodel.dml
+++ datamodel.dml
@@ -1,15 +1,15 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Brand {
-  id          String    @default(cuid()) @id
+  id          String    @default(uuid()) @id
   name        String
   slug        String    @unique
   description String?
   founded     DateTime?
@@ -19,9 +19,9 @@
   @@map("brand")
 }
 model Category {
-  id          String    @default(cuid()) @id
+  id          String    @default(uuid()) @id
   name        String
   slug        String    @unique
   description String?
   parentId    String?   @map("parent_id")
@@ -31,9 +31,9 @@
   @@map("category")
 }
 model Product {
-  id          String      @default(cuid()) @id
+  id          String      @default(uuid()) @id
   name        String
   slug        String      @unique
   description String?
   active      Boolean     @default(true)
@@ -85,9 +85,9 @@
   LOST
 }
 model Inventory {
-  id                 String             @default(cuid()) @id
+  id                 String             @default(uuid()) @id
   serial             String?
   sku                String?
   active             Boolean            @default(true)
   condition          InventoryCondition @default(NEW)
@@ -108,9 +108,9 @@
   @@map("inventory")
 }
 model Bin {
-  id        String        @default(cuid()) @id
+  id        String        @default(uuid()) @id
   cell      Int
   column    Int
   row       Int
   location  Int
@@ -123,9 +123,9 @@
   @@map("bin")
 }
 model BinFreeNode {
-  id      String   @default(cuid()) @id
+  id      String   @default(uuid()) @id
   x       Float
   y       Float
   width   Float
   height  Float
@@ -136,9 +136,9 @@
   @@map("bin_free_node")
 }
 model Address {
-  id              String     @default(cuid()) @id
+  id              String     @default(uuid()) @id
   street1         String
   street2         String?
   city            String
   state           String
@@ -158,9 +158,9 @@
   @@map("address")
 }
 model Warehouse {
-  id              String   @default(cuid()) @id
+  id              String   @default(uuid()) @id
   street1         String
   street2         String?
   city            String
   state           String
@@ -200,9 +200,9 @@
   EARN
 }
 model Shipment {
-  id                 String            @default(cuid()) @id
+  id                 String            @default(uuid()) @id
   pickup             Boolean           @default(false)
   airbox             Boolean           @default(false)
   expedited          Boolean           @default(false)
   direction          ShipmentDirection @default(OUTBOUND)
@@ -234,9 +234,9 @@
   @@map("shipment")
 }
 model Order {
-  id        String      @default(cuid()) @id
+  id        String      @default(uuid()) @id
   products  Product[]   @relation("order_product", references: [id])
   inventory Inventory[] @relation("order_inventory", references: [id])
   created   DateTime    @default(now())
   canceled  DateTime?
```


