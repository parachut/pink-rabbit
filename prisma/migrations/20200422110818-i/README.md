# Migration `20200422110818-i`

This migration has been generated by Daniel Wallace at 4/22/2020, 11:08:18 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."address" DROP COLUMN "carrier_facility",
DROP COLUMN "easy_post_id",
ALTER COLUMN "phone" DROP NOT NULL,
ALTER COLUMN "residential" SET DEFAULT false;

DROP TABLE "public"."products_social_posts";

DROP TABLE "public"."product_values";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200410123953-r..20200422110818-i
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -138,25 +138,23 @@
   @@map("bin_free_node")
 }
 model Address {
-  id              String     @default(uuid()) @id
-  street1         String
-  street2         String?
-  city            String
-  state           String
-  zip             String
-  country         String     @default("US")
-  residential     Boolean
-  carrierFacility String     @map("carrier_facility")
-  name            String
-  company         String?
-  phone           String
-  email           String
-  created         DateTime   @default(now())
-  easyPostId      String     @map("easy_post_id")
-  userId          String     @map("user_id")
-  shipments       Shipment[]
+  id          String     @id
+  street1     String
+  street2     String?
+  city        String
+  state       String
+  zip         String
+  country     String     @default("US")
+  residential Boolean    @default(false)
+  name        String
+  company     String?
+  phone       String?
+  email       String
+  created     DateTime   @default(now())
+  userId      String     @map("user_id")
+  shipments   Shipment[]
   @@map("address")
 }
```


